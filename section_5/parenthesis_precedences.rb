def authenticate(rank, name, cred)
    if (rank == "007" && name == "James Bond") || cred == "Secret Agent"
        puts "This is the real agent!1!1!"
    else
        puts "minggattt"
    end
end

authenticate("001","mark", "Spy" )